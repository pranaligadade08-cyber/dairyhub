# Copy to .env and add your key for full AI answers (optional).
# Without this file, the app still answers common dairy questions locally.
OPENAI_API_KEY=

# Public site URL for QR codes (no trailing slash), e.g. https://your-domain.com
# PUBLIC_SITE_URL=http://127.0.0.1:8000
